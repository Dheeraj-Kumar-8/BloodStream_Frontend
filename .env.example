# Client environment variables for Render
# Set this in Render Static Site -> Environment tab

# Backend API base URL including /api
# Example: https://bloodstream-backend.onrender.com/api
VITE_API_URL=
# API Base URL (for production deployment)
# When deploying to a domain, set this to your backend API URL
# Example: VITE_API_URL=https://api.yourbloodstream.com
VITE_API_URL=http://localhost:5000

# Socket.IO URL (optional, defaults to window.location.origin)
# Only needed if Socket.IO is on a different domain than the frontend
# VITE_SOCKET_URL=https://api.yourbloodstream.com
